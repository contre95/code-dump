'''
cols = []
rows = []
for row in matrix:
    for col in row:
        if col==0:
            cols.append(col)
            rows.append(row)

for row in matrix:
    if row in rows;
        for col in range(len(row)):
            row[col] = 0
    else:
        for col in range(len(row)):
            if(row[col] in cols):
                row[col] = 0
'''

